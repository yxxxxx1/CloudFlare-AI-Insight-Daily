name: Auto Build AI Daily & Deploy Pro Website

on:
  workflow_dispatch: {}
  schedule:
    # 每天北京时间 08:10 运行（UTC 00:10）
    - cron: "10 0 * * *"

jobs:
  auto-daily:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        # [优化] 添加 fetch-depth: 0 以便 Hugo/Hextra 能获取所有 .md 历史
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # --- [第 1 部分：核心日报生成 - 你的原始代码，完全不变] ---
      - name: Run end-to-end pipeline (login → writeData → genAI → commit → writeRssData → rss)
        env:
          BASE_URL: ${{ vars.WORKER_BASE_URL }}
          USERNAME: ${{ secrets.WORKER_LOGIN_USERNAME }}
          PASSWORD: ${{ secrets.WORKER_LOGIN_PASSWORD }}
          FOLO_COOKIE_SECRET: ${{ secrets.CF_FOLO_COOKIE }}
          MAX_PER_TYPE: "6"
          TZ: Asia/Shanghai
        run: |
          node - <<'EOF'
          // -------- helpers ----------
          const BASE = process.env.BASE_URL.replace(/\/+$/, '');
          const DATE = new Date(Date.now() + 8*3600*1000).toISOString().slice(0,10); // GMT+8 yyyy-mm-dd
          const MAX_PER_TYPE = Number(process.env.MAX_PER_TYPE || 6);

          let cookieJar = '';
          const keepCookie = (res) => {
            const set = res.headers.get('set-cookie');
            if (set) {
              const one = set.split(',')[0];
              const m = one.match(/^[^;]+/);
              if (m) {
                cookieJar = cookieJar ? (cookieJar + '; ' + m[0]) : m[0];
              }
            }
            return cookieJar;
          };

          const fetchWithCookie = async (url, opts={}) => {
            const headers = Object.assign({}, opts.headers || {});
            if (cookieJar) headers['cookie'] = cookieJar;
            const res = await fetch(url, {...opts, headers});
            keepCookie(res);
            return res;
          };

          const ok = (res) => {
            if (!res.ok) throw new Error(`HTTP ${res.status} ${res.statusText}`);
          };

          const form = (pairs) => {
            const fd = new FormData();
            for (const [k,v] of pairs) {
              if (Array.isArray(v)) v.forEach(x=>fd.append(k, x));
              else fd.append(k, v);
            }
            return fd;
          };

          console.log(`BASE: ${BASE}`);
          console.log(`DATE: ${DATE}`);

          // 1) login 获取 cookie
          console.log('🔐 Login ...');
          {
            const fd = new URLSearchParams();
            fd.set('username', process.env.USERNAME);
            fd.set('password', process.env.PASSWORD);
            const res = await fetch(`${BASE}/login`, {
              method: 'POST',
              headers: { 'content-type': 'application/x-www-form-urlencoded' },
              redirect: 'manual',
              body: fd.toString()
            });
            if (res.status !== 200 && res.status !== 302) {
              throw new Error(`Login failed: HTTP ${res.status}`);
            }
            keepCookie(res);
            if (!cookieJar) throw new Error('No cookie returned from login');
          }
          console.log('✅ Login OK');

          // 2) writeData
          console.log('📝 writeData ...');
          {
            const res = await fetchWithCookie(`${BASE}/writeData`, {
              method: 'POST',
              headers: { 'content-type': 'application/json' },
              body: JSON.stringify({ foloCookie: process.env.FOLO_COOKIE_SECRET })
            });
            ok(res);
            const j = await res.json().catch(()=>({}));
            console.log('writeData result:', j);
          }

          // 3) getContent
          console.log('📥 getContent ...');
          const all = {};
          {
            const res = await fetch(`${BASE}/getContent?date=${DATE}`);
            ok(res);
            const j = await res.json();
            for (const [k,v] of Object.entries(j)) {
              if (Array.isArray(v)) all[k] = v;
            }
            console.log('got categories:', Object.keys(all));
          }

          // 4) 构造 selectedItems
          console.log('🧩 build selectedItems ...');
          const selectedItems = [];
          for (const [type, arr] of Object.entries(all)) {
            for (const it of (arr || []).slice(0, MAX_PER_TYPE)) {
              if (it && (it.id !== undefined && it.id !== null)) {
                selectedItems.push(`${type}:${it.id}`);
              }
            }
          }
          if (selectedItems.length === 0) throw new Error('No items to select – writeData may have fetched nothing.');

          // 5) genAIContent（JSON 模式）
          console.log('🤖 genAIContent (JSON mode)...');
          let markdown = '';
          {
            const fd = form([
              ['date', DATE],
              ['selectedItems', selectedItems]
            ]);

            const res = await fetchWithCookie(`${BASE}/genAIContent?mode=json`, {
              method: 'POST',
              body: fd
            });
            ok(res);

            const j = await res.json().catch(() => null);
            if (!j || !j.markdown) {
              console.error('Full response:', j);
              throw new Error('genAIContent JSON response invalid or missing markdown field.');
            }

            markdown = j.markdown;
            if (!markdown.trim()) throw new Error('Empty markdown in genAIContent JSON result.');
          }
          console.log(`✅ genAIContent OK (JSON mode, length=${markdown.length})`);

          // 6) commitToGitHub
          console.log('⬆️ commitToGitHub ...');
          {
            const fd = form([
              ['date', DATE],
              ['daily_summary_markdown', markdown]
            ]);
            const res = await fetchWithCookie(`${BASE}/commitToGitHub`, {
              method: 'POST',
              body: fd
            });
            ok(res);
            const j = await res.json().catch(()=>({}));
            console.log('commitToGitHub result:', j);
          }

          // 7) writeRssData
          console.log('🪄 writeRssData ...');
          {
            const res = await fetch(`${BASE}/writeRssData?date=${DATE}`, { redirect: 'follow' });
            ok(res);
            const j = await res.json().catch(()=>({}));
            console.log('writeRssData result:', j.title || j.report_date || 'OK');
          }

          // 8) 下载 RSS 到本地
          console.log('⬇️ download RSS ...');
          {
            const res = await fetch(`${BASE}/rss?days=7`);
            ok(res);
            const xml = await res.text();
            const fs = await import('node:fs/promises');
            await fs.writeFile('rss.xml', xml, 'utf8');
          }

          console.log('🎉 All done for', DATE);
          EOF

      # --- [第 2 部分：提交 RSS 并拉取最新 MD - 你的原始代码，完全不变] ---
      # (这个步骤会拉取 Worker 提交的 .md 文件，并提交 rss.xml)
      - name: Commit and push changes (rss.xml)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          if [ -f "rss.xml" ]; then
            git add rss.xml
          fi

          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "docs: auto-build AI Daily ($(date -u))"
            # 先拉取远程更新再推送，防止 fetch first 冲突
            # 这一步会拉取第 1 部分中 Worker 提交的 .md 文件
            git pull --rebase origin main || true
            git push origin main
          fi

      # --- [第 3 部分：构建专业网站 - 替换原有的部署步骤] ---
      - name: Install Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: 'latest'

      - name: Setup Hugo Theme (Hextra)
        run: |
          echo "Cloning Hextra theme..."
          mkdir -p themes
          # 使用 Git Submodule 来安装主题
          git submodule add -f https://github.com/imfing/hextra.git themes/hextra

      - name: Build Website with Hugo
        run: |
          echo "Creating Hugo configuration..."
          # ... (hugo.toml, menu, 等配置都保持不变) ...
          cat << EOF > hugo.toml
          baseURL = "https://yxxxxx1.github.io/CloudFlare-AI-Insight-Daily/"
          languageCode = "zh-cn"
          title = "AI 资讯日报"
          theme = "hextra"

          [params]
            [params.theme]
              mode = "auto"
              toggle = true
          
          [menu]
            [[menu.main]]
              name = "历史日报"
              url = "/daily"
              weight = 0
            [[menu.main]]
              name = "RSS"
              url = "/rss.xml"
              weight = 1
            [[menu.main]]
              name = "GitHub"
              url = "https://github.com/yxxxxx1/CloudFlare-AI-Insight-Daily"
              weight = 2
          EOF

          echo "Organizing content for Hugo..."
          mkdir -p content/daily
          cp -r daily/*.md content/daily/
          mkdir -p static
          cp rss.xml static/
          
          echo "Creating daily list page..."
          cat << EOF > content/daily/_index.md
          ---
          title: "历史日报"
          type: "docs" 
          draft: false
          ---

          这里是所有的历史日报存档。
          EOF
          
          # --- ▼▼▼【最终修复：移除内容中的重复标题】▼▼▼ ---
          echo "Fixing missing Front Matter for Hugo..."
          for file in content/daily/*.md; do
            if [[ "$file" == "content/daily/_index.md" ]]; then
              continue
            fi
            if [ "$(head -n 1 "$file")" != "---" ]; then
              echo "Adding Front Matter to $file"
              
              date_from_file=$(basename "$file" .md)
              title_from_file=$(head -n 1 "$file" | sed 's/^## //' | sed 's/[[:space:]]*$//')
              
              # 【修复】使用 `tail -n +2` 来获取 *除了第一行之外* 的所有内容
              content_without_title=$(cat "$file" | tail -n +2) 

              (
                echo "---"
                echo "title: \"$title_from_file\""
                echo "date: $date_from_file"
                echo "type: \"docs\"" 
                echo "draft: false"
                echo "---"
                echo ""
                # 【修复】只写入不含标题的内容
                echo "$content_without_title"
              ) > "$file"
            else
              echo "Skipping $file, Front Matter already exists."
            fi
          done
          # --- ▲▲▲【修复结束】▲▲▲ ---

          # --- ▼▼▼【最终重定向方案 (保持不变)】▼▼▼ ---
          echo "Finding latest post and creating redirect layout..."
          
          LATEST_FILE_PATH=$(ls -1 daily/*.md | grep -v '_index.md' | sort -r | head -n 1)
          LATEST_BASENAME=$(basename "$LATEST_FILE_PATH")
          LATEST_POST_SLUG=$(echo "$LATEST_BASENAME" | sed 's/\.md$//')
          REDIRECT_URL="/CloudFlare-AI-Insight-Daily/daily/${LATEST_POST_SLUG}/"

          echo "Latest post slug is: $LATEST_POST_SLUG"
          echo "Homepage will redirect to: $REDIRECT_URL"

          mkdir -p layouts
          cat << EOF > layouts/index.html
          <!DOCTYPE html>
          <html lang="zh-cn">
          <head>
          <title>AI 资讯日报</title>
          <meta http-equiv="refresh" content="0; url=${REDIRECT_URL}">
          <link rel="canonical" href="${REDIRECT_URL}">
          </head>
          <body>
          <p>正在跳转到<a href="${REDIRECT_URL}">最新的 AI 资讯日报</a>...</p>
          </body>
          </html>
          EOF
          
          cat << EOF > content/_index.md
          ---
          title: "AI 资讯日报"
          draft: false
          ---
          EOF
          # --- ▲▲▲【重定向结束】▲▲▲ ---
          
          echo "Running Hugo build..."
          hugo -D --buildFuture

      # --- [第 4 部分：部署到 gh-pages 分支] ---
      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # 指定 Hugo 的输出目录
          publish_dir: ./public
          # 确保推送到 gh-pages 分支
          publish_branch: gh-pages
          # 每次都强制推送，覆盖旧的构建
          force_orphan: true
